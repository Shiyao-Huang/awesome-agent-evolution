# Hourly Public Metadata Update - 2026-05-25 14:23

## One Sentence

Added four memory/context benchmark substrate records to keep the public metadata loop current without claiming API freshness.

## Three Sentences

The new raw captures are EvoMemBench, Signet AI, MemPalace, and RASPUTIN Memory. They extend the memory/evaluation side of the self-evolution corpus and produce public model cards 192-195 plus site report copies. GitHub API freshness remains blocked by shell DNS and invalid local gh auth, so all new snapshots are explicitly web-observed.

## Five Details

1. EvoMemBench is important because it ties agent memory benchmarking to a self-evolving perspective and an arXiv 2026 paper.
2. Signet is important because it frames context as a portable raw-record/semantic-memory/query-layer substrate across harnesses including Codex.
3. MemPalace is included as a high-attention benchmark-claim record, with the benchmark numbers treated as project claims until independently rerun.
4. RASPUTIN Memory is useful as a lower-star but evidence-honest self-hosted memory backend that separates shipped and not-shipped features.
5. GitNexus was available and rerun in a fresh temp HOME; graph search is usable, while FTS remains degraded because the LadybugDB extension cannot download.

## Evidence And Limits

- Web evidence: GitHub pages for the four repos plus arXiv 2605.18421 for EvoMemBench.
- API blocker: `curl https://api.github.com/rate_limit` failed DNS resolution; `gh auth status` reports invalid token.
- Validation plan: regenerate indexes, enforce timestamps, recompute GitHub analysis, refresh visuals, build site, and rebuild paper because analysis TeX changed.
