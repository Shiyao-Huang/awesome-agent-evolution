# Mem0 Model Card

| Field | Value |
|---|---|
| Repository | [mem0ai/mem0](https://github.com/mem0ai/mem0) |
| Category | Agent 通用记忆层 |
| Stars / forks snapshot | 56,500 / 6400 |
| Language | Python |
| License | Apache-2.0 |
| Raw capture | raw-github/mem0ai_mem0.md |
| Updated by | hourly public metadata update, 2026-05-24 |

## 1. Role in Self Evolve

通用 AI Agent 记忆层，提供用户、会话和 Agent 状态记忆，以及 OpenClaw、CLI、SDK 和 evaluation 支持。

## 2. Working Principle

把跨会话事实、偏好和 agent state 写入可检索记忆层，通过语义、关键词、实体和时间信号融合召回。

## 3. Evidence Path

web GitHub page observed memory/plugin/evaluation directories and README benchmark claims; shell GitHub API was blocked by DNS and gh auth was invalid, so this run marks freshness as web-page observed rather than API verified.

## 4. Teaching Use

Use this card to explain whether a repository is a runtime, benchmark, harness-evolution loop, memory/skill substrate, or resource index. The key reading path is: raw capture -> classification row -> public site card -> project report -> aggregate GitHub analysis.

## 5. Limits

当前未克隆源码，未运行 benchmark；star/fork/commit 快照来自公开 GitHub 页面文本。
